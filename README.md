# contao-news-json-ld-types-bundle

This bundle allows you to select the JSON-LD types Article, NewsArticle and BlogPosting for Contao news articles instead of always using NewsArticle.

After installing just select the desired JSON-LD type in the news archive settings, the JSON-LD output will then be adjusted accordingly. 